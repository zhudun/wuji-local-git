# 项目 AI 处理规则（Cursor / Copilot 等 AI 助手适用）

## 请先阅读此文件

本文件是给 AI 助手的上下文说明。在处理本项目的任何代码之前，请先理解以下背景和约束。

---

## 项目背景

本项目的前端代码来源于 **腾讯无极（Wuji）低代码平台**，通过下载到本地后用 Git 进行版本管理。

### 无极平台的特殊性

- 无极是一个低代码/零代码开发平台，代码由平台生成或在平台内编辑
- 平台本身 **没有 Git**，只有一个简陋的历史提交功能
- 代码可以下载到本地进行开发，但 **每次重新连接都会生成新的本地文件夹**
- 因此本仓库使用 `wuji-sync.sh` 脚本将每次下载的新文件夹同步进来

### 代码管理流程

```
无极平台 → 下载到临时文件夹 → wuji-sync.sh 同步 → 本 Git 仓库
本 Git 仓库 → 手动复制修改 → 无极平台
```

---

## 硬性约束（必须遵守）

1. **代码绝对不能上传到任何外部服务**
   - 不能推送到 GitHub、GitLab（外网）、Gitee 或任何公网仓库
   - 不能使用任何涉及代码上传的第三方服务
   - 公司内网 GitLab 也没有创建仓库的权限，不要建议使用
   - 所有操作必须纯本地完成

2. **不要建议使用远程 Git 服务**
   - 不要在代码或配置中添加 remote 地址
   - 不要生成 `git push`、`git remote add` 等涉及远程的命令
   - 备份只能用本地方式（如 `git bundle`、复制到其他磁盘）

3. **企业保密要求**
   - 不要在任何输出中暴露项目的具体业务逻辑描述
   - 不要生成包含公司/项目信息的外部请求
   - 所有 AI 生成的代码和建议都只用于本地开发

---

## 项目结构理解

### 工具文件（不是业务代码，不要修改除非被要求）

| 文件 | 用途 |
|------|------|
| `wuji-sync.sh` | 从无极下载目录同步代码到本仓库的脚本 |
| `.wuji-sync-ignore` | 同步时需要排除的文件/目录规则 |
| `.cursorrules` | 本文件，AI 处理规则 |
| `README.md` | 工作流说明文档 |

### 业务代码（同步进来的无极项目文件）

业务代码是通过 `wuji-sync.sh` 从无极平台同步进来的。这些代码的特点：

- 可能包含无极平台自动生成的代码，结构可能不符合常规前端项目规范
- 可能有平台特有的配置文件、模板语法或运行时依赖
- 变量命名、文件组织可能是平台生成的风格，不一定遵循社区最佳实践
- **修改业务代码时要保持与无极平台的兼容性**，不要引入平台不支持的语法或特性

---

## AI 处理代码时的注意事项

### 1. 修改代码前

- 先了解文件是工具文件还是业务代码
- 业务代码要注意无极平台的兼容性
- 如果不确定某个写法是否是无极平台特有的，保持原样不要改

### 2. 代码风格

- 保持与现有代码一致的风格，不要强行改为"最佳实践"
- 无极生成的代码可能有特定的命名规范或结构约定，尊重这些约定
- 如果代码中有 `/* wuji: ... */` 或类似的平台标记注释，不要删除

### 3. 依赖管理

- 不要随意添加新的 npm 包，需要确认无极平台是否支持
- 如果无极平台有自己的组件库或 SDK，优先使用平台提供的
- 不要修改平台相关的配置文件（如构建配置、部署配置等），除非明确被要求

### 4. 生成代码时

- 所有生成的代码、脚本、配置都必须是纯本地可用的
- 不要生成任何包含 API Key、Token 上传或远程服务调用的代码（除非是项目本身需要的业务接口）
- 写注释用中文（与团队保持一致），除非是代码内的英文标识符

### 5. Git 操作建议

- 只建议本地 Git 操作：`git add`、`git commit`、`git log`、`git diff`、`git branch` 等
- 备份建议只用 `git bundle` 或文件复制
- 不要建议 `git push`、`git pull`、`git remote` 等远程操作

---

## 常见任务的处理方式

### "帮我看看这次无极同步了什么变更"

```bash
# 同步后查看变更
git diff --cached --stat     # 变更文件列表
git diff --cached            # 详细变更内容
```

### "帮我对比某两个版本的区别"

```bash
git log --oneline            # 先看提交历史
git diff <commit1> <commit2> # 对比两个版本
```

### "帮我恢复某个文件到之前的版本"

```bash
git log --oneline -- path/to/file    # 查看文件历史
git checkout <commit> -- path/to/file # 恢复到指定版本
```

### "帮我备份当前仓库"

```bash
git bundle create backup-$(date +%Y%m%d).bundle --all
# 把生成的 .bundle 文件复制到安全的本地位置（U盘、其他磁盘）
```

### "帮我整理提交历史"

```bash
# 交互式 rebase 合并/整理最近的提交（本地操作，安全）
git rebase -i HEAD~5
```
